package com.example.a2dgamedemo.Presentation.Classes.Common

import android.content.Context
import com.example.a2dgamedemo.Enums.DensityTypes
import com.example.a2dgamedemo.Presentation.Classes.Map.TileMap

open class MapObject(var context : Context, var map :TileMap, var mapSize : DensityTypes, var density : Int ) {
    /*private lateinit var context : Context
    private lateinit var map : TileMap
    private lateinit var mapSize : DensityTypes
    private var density = 0*/
}